export type VarsType = {
  palette: {
    text: {
      secondary: string;
      primary: string;
    };
    action: {
      selected: string;
    };
    divider: string;
    background: {
      defaultChannel: string;
      default: string;
    };
  };
  shape: {
    borderRadius: string;
  };
  shadows: string[];
};
